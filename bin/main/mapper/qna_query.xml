<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.happyhouse.mapper.QnAMapper">

	<!-- QnA 전체 조회 -->
	<select id="selectQnA" resultType="QnA">
		select qna_no as qnaNo, 
			   qna_title as qnaTitle,
			   qna_content as qnaContent,
			   qna_userid as qnaUserid,
			   qna_datetime as qnaDatetime			   
		from qna_board
		 order by qna_no desc
	</select>
	
	<!-- QnA 상세 조회 -->
	<select id="detailQnA" resultType="QnA">
		select qna_no as qnaNo, 
			   qna_title as qnaTitle,
			   qna_content as qnaContent,
			   qna_userid as qnaUserid,
			   qna_datetime as qnaDatetime
		from qna_board
		 where qna_no = #{qnaNo}
	</select>
<!-- , -->
<!-- 			   reply_content as replyContent, -->
<!-- 			   reply_datetime as replyDatetime, -->
<!-- 			   reply_userid as replyUserid -->
	<!-- QnA 등록 -->
	<insert id="insertQnA" parameterType="QnA">
		insert into qna_board(
			qna_title, qna_content, qna_userid,qna_datetime
		) values (
			#{qnaTitle}, #{qnaContent}, #{qnaUserid}, now()
		)
	</insert>
	
	<!-- QnA 수정 -->
	<update id="updateQnA" parameterType="QnA">
		update qna_board 
		   set qna_title = #{qnaTitle}, 
		   	   qna_content = #{qnaContent}
		 where qna_no = #{qnaNo}
	</update>

	<!-- QnA 삭제 -->
	<delete id="deleteQnA" parameterType="int">
		delete 
		  from qna_board 
		 where qna_no = #{qnaNo}
	</delete>
	
<!-- 	<select id="searchQnA" resultType="qna"> -->
<!-- 		<if test="key eq 'qnaNo'.toString()" > -->
<!-- 			select * from qna_board where qna_no = #{word}; -->
<!-- 		</if> -->
<!-- 		<if test="key eq 'qnaTitle'.toString()" > -->
<!-- 			select * from qna_board where qna_title like CONCAT('%', #{word}, '%'); -->
<!-- 		</if> -->
<!-- 	</select> -->

</mapper>

